# DECISIONS — house rules already made

Inherit these. To change one, add a new entry that supersedes it; never edit history.

| ID | Date | Decision | Why | Status |
|---|---|---|---|---|
| D-01 | 2026-09-22 | Engine is Python 3.12 + FastAPI + Pydantic v2 with `hyperon==0.2.10` loaded in-process. The README's earlier Node/TypeScript server and sentinel protocol are retired. | PRD §6; in-process runtime verified in the Architect session; one process to demo. | Active |
| D-02 | 2026-09-22 | Marketplace data lives in Convex. | Operator decision 2026-09-22 (morning). | **Superseded by D-17** |
| D-03 | 2026-09-22 | Auth and roles: Clerk. Role (`founder` \| `builder` \| `admin`) is chosen at sign-up and stored in Clerk `publicMetadata.role`; the FastAPI marketplace API verifies the Clerk JWT and reads the role claim; a Clerk `user.created`/`user.updated` webhook upserts the `users` row. First admin is bootstrapped by `ADMIN_EMAILS` env var in the engine. | Roles available client-side without a round trip; PRD §6 Auth row. Amended by D-17. | Active |
| D-04 | 2026-09-22 | Routing is public. `POST /api/conversation` needs no session. Clerk gates only marketplace screens (profile, projects, requests, bids, bookings, admin queue). | Judges reach a route in under two minutes; demo floor decoupled from auth. | Active |
| D-05 | 2026-09-22 | Deploy: web on Vercel; engine Docker image on Railway (Render is the fallback); Neon Postgres for the database. Local: `docker compose up` runs `engine`, `db` (postgres:16) and `web`; Neon branches for demo resets. | Operator decision; amended by D-17. Vercel cannot host the Python runtime. | Active |
| D-06 | 2026-09-22 | LLM: Anthropic `anthropic` Python SDK, model `claude-opus-5`, structured output via `output_config.format`, server-side key, adapter interface `LlmAdapter` so a second provider can be added without touching orchestration. Form fallback ships in Sprint 001. | Operator holds an Anthropic key. | Active |
| D-07 | 2026-09-22 | Headline multi-hop chain includes a `partners-with (partner, university)` edge: brief vertical → `supports-vertical` partner → `partners-with` university → `cohort-of` cohort → `belongs-to` builder. Four hops in one `partner-fit` query. | One-hop vertical lookup is unconvincing to a MeTTa judge. | Active |
| D-08 | 2026-09-22 | Availability: builder interval qualifies when it overlaps the brief interval by at least `MIN_OVERLAP_DAYS = 2` (engine constant, not a brief field). Demo briefs span 7 days. Both brief and profile use a shadcn Calendar range picker. | Operator: "one week or two days", calendar UI. Resolves PRD open question 1. | Active |
| D-09 | 2026-09-22 | Gap ownership: MeTTa `route-gap` emits `skill`, `availability`, `mode`, `location` gaps per required skill. The Python assembler emits `team-size` and `budget` gaps under the named rules `assembler.team-size-fit` and `assembler.budget-fit`. Status is a pure function: all skills covered and a subset survives → `feasible`; some skill covered or any gap → `partial`; no builder eligible for any skill → `infeasible`. The LLM writes the summary and explains the status and next actions; it never sets the status value. | Resolves PRD open question 2. ASSUMPTION flagged to Operator: Operator said "use LLM to emit partial or infeasible based on route-gap"; the Architect read this as "explain", because a model-chosen status would break PRD §5.7 and the judge test. | Active — confirm |
| D-10 | 2026-09-22 | Sprint numbering follows the PRD (000 spike … 005 hardening), not the kit's 001-planning convention. | Already in README and milestones. | Active |
| D-11 | 2026-09-22 | Sprint 000 is time-boxed to half a day of Builder time. Abort criterion: if `pytest -m runtime` cannot pass through FastAPI by the box, escalate to a MeTTa mentor and shrink the rule set; never swap in a matcher. | Runtime already proven in the Architect session. | Active |
| D-12 | 2026-09-22 | Builder = Claude Code with Matt Pocock skills (`to-tickets`, `implement`, `tdd`, `code-review`); one branch and one PR per sprint; Operator merge is the gate to the next sprint. Sprints are gated by timeline **and** acceptance (`TIMELINE.md`). | Operator decision. | Active |
| D-13 | 2026-09-22 | Design source is Google Stitch via the prompt pack in `docs/design/stitch-prompts.md`; exports are committed under `design/stitch/` and converted to React by the Builder. | Operator decision. | Active |
| D-14 | 2026-09-22 | Demo clock is frozen at `2026-09-22` for seed data (`DEMO_TODAY` env var) so availability windows stay deterministic in tests and on demo day. | Deterministic scenarios. | Active |
| D-15 | 2026-09-22 | Graph projection: engine loads seed facts from `services/engine/seed/*.metta` at start-up, then reads confirmed rows (`profiles`, `credentials`, `projects`, `availability`) from Postgres and adds their atoms. Every admin confirmation or rejection calls `reproject()` in-process after commit. Reprojection is full-rebuild, not incremental, for this release. | Resolves PRD open question 4; full rebuild is cheap at demo scale. Amended by D-17. | Active |
| D-16 | 2026-09-22 | Single demo currency USD, integer per day. Timezone Africa/Nairobi for all date-only fields. Python 3.12 pinned (no hyperon wheel for 3.13). | PRD §5.3, PyPI wheel list. | Active |
| D-17 | 2026-09-22 | **Marketplace store is Neon Postgres** through the FastAPI service: SQLModel models, Alembic migrations, `asyncpg`, one `DATABASE_URL` (pooled). Local dev uses the compose `db` service; CI and demo use a Neon branch. No Convex, no Prisma, no Node marketplace API. Supersedes D-02 and amends D-03, D-05, D-15. | Operator decision 2026-09-22 (afternoon): "we are going to use Neon". One backend language; PRD §6 as written. | Active |
| D-18 | 2026-09-22 | Builder process uses Matt Pocock's skills exactly as designed: `/setup-matt-pocock-skills` once (GitHub Issues tracker, `docs/agents/`, root `CONTEXT.md` + `docs/adr/`), then per sprint `/to-tickets` on `requirements.md`, `/implement` per ticket (which runs `tdd` at pre-agreed seams and `code-review`), `/code-review master` before the PR. `to-spec` is skipped because `requirements.md` is the spec. The exact prompts live in `planning/PROMPTS.md`. | The skills are `disable-model-invocation`; only the Operator can invoke them. | Active |
| D-20 | 2026-09-22 | Delivery-mode policy table `mode-accepts` in `rules.metta`: a `remote` brief accepts builders supporting `remote` or `hybrid`; a `hybrid` brief accepts `remote` or `hybrid`; an `on-site` brief accepts only `on-site` supporters whose `located-in` equals the brief location. | Ratifies the Sprint 000 Builder reading (Q-10); Operator said "continue". | Active |
| D-21 | 2026-09-22 | `route-gap` category for an on-site brief is `location` when a verified, available builder supports on-site elsewhere, otherwise `mode`. Helper equations `verified-and-available`, `verified-available-compatible`, `on-site-elsewhere` are not rule names and are excluded from `rules_loaded`. | Ratifies Q-11. | Active |
| D-22 | 2026-09-22 | Budget gap shape: when every covering subset exceeds `dailyBudget`, status is `partial`, `builders` is empty, the single gap is `{category: budget, rule: assembler.budget-fit, affected: [ids of the cheapest covering subset], statement: "Cheapest verified team costs USD <total> a day; budget is USD <budget>", nextActions: ["Raise daily budget to USD <total>"]}`. Team-size gap mirrors this with `assembler.team-size-fit` and "Raise maximum team size to <n>". | Makes the Budget scenario deterministic and honest: no partial team is shown as if affordable. | Active |
| D-23 | 2026-09-22 | `infeasible` (no builder eligible for any required skill) returns `builders: []`, no `reusableIp`, no `cohort`, no `partner`, and one gap per required skill. `partial` and `feasible` include `reusableIp` from `reuse-fit` when `preferReusableIp`, `cohort` of the first selected builder, and the `partner-fit` candidate for that builder. The Delivery-mode scenario (on-site Kisumu) is therefore `infeasible` with three `location` gaps. | Aligns DOMAIN.md scenarios with D-09 and Sprint 002's infeasible screen. | Active |
| D-24 | 2026-09-22 | Builder display names are derived from the builder ID by one presenter helper (`amina-otieno` → "Amina Otieno"); the seed carries no name facts. Day rates are read through one engine method `day_rates(builder_ids)` over `day-rate` atoms; the assembler receives them as plain input. Currency and unit stay USD per day (D-16); there are no hourly rates anywhere in this project. | Sprint 001 ticket breakdown questions; keeps the seed small and the assembler pure. | Active |
| D-19 | 2026-09-22 | Test seams are fixed per sprint in `planning/PROMPTS.md` (engine: `MettaRouteEngine` methods and the HTTP endpoints; web: rendered screens via RTL/Playwright; marketplace: HTTP endpoints with a Neon test branch). `tdd` tests only at these seams. | `tdd` skill rule: no test at an unconfirmed seam. | Active |
