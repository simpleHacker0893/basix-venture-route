# PROMPTS-005 — paste-ready prompts for Sprint 005 (Builder Showcase, then demo hardening)

Same rules as `planning/PROMPTS.md`: **you** type the slash commands (`/to-spec`, `/to-tickets`, `/implement`, `/code-review` are `disable-model-invocation`), tests are written only at the seams fixed below (D-19), and every part ends with `verification-before-completion` evidence, not a claim. One Claude Code session per part. The generic P1–P5 wording lives in `planning/PROMPTS.md` §"The five prompts you paste in every sprint"; this file gives the Sprint 005 lines in full so nothing has to be assembled by hand.

Windows (D-42, `planning/TIMELINE.md`): **Part A** Fri 25 Sep 08:00 → Sun 27 Sep 20:00 EAT, branch `sprint/005a-showcase`, PR "Sprint 005a: Showcase". **Part B** Mon 28 Sep 08:00 → Wed 30 Sep 20:00, freeze 22:00, branch `sprint/005-demo-hardening`, PR "Sprint 005: Demo hardening". Demo Thu 1 Oct.

---

## What Sprint 005 inherits from Sprint 004 (read before P1)

Sprint 004 merged into `master` with PR #78 (Requests and interviews; spec #52, tickets #53–#77). The close-out review is `planning/sprints/004-requests-interviews/review.md`; the decisions it recorded are D-44 to D-49.

**Engine (`services/engine`)**
- Tables (migration `0002`): `requests` (a founder's published brief with its route snapshot as JSONB), `bids` (one per builder per request, UNIQUE), `bookings` (state, JSONB history, proposed start in UTC, duration 30 or 45), every row `demo_data` default true + CHECK. Migration `0003_showcase` is the next number.
- `RouteService.eligibility(brief, builder_id)` (`app/routing/eligibility.py`): the two-tier reason over `eligible_builders` and `gaps` only, never a matcher (D-45). `POST /api/requests/{id}/bids` answers 403 with that reason verbatim; the board's Bid button shows the same text.
- Booking machine `app/marketplace/booking.py`: pure `start` / `check` / `transition` over `(state, action, actor)`; founder-owned (D-44); one counter per round per side, a founder counter opens a new round; `check` runs before body validation so an illegal cell is 409 before a bad slot is 422. Slot rules and the Africa/Nairobi presenter in `app/marketplace/slots.py` (D-46). History is JSONB on the row, appended in the same transaction as the state change (D-48).
- `GET /api/me/dashboard`: SQL `count(*)` tiles, requests, bids received, upcoming bookings judged against an injectable clock (`create_app(clock=…)`; tests inject `TEST_NOW`).
- Admin: `GET /api/admin/pending` and `/decided`, `POST /api/admin/{confirm|reject}/{kind}/{id}` (kinds `account | credential | project`; Sprint 005a adds `showcase`), reprojection after every decision (D-15). `#49` (Reverse from `/admin`) ships separately and is a 005a Should line.
- Test fixtures: `tests/conftest.py` cast (`founder`, `admin`, `confirmed_builder`, `unconfirmed_builder`, `rejected_builder`, `Actor` with `.headers`), `TEST_NOW`, the compose `db` at `postgresql+asyncpg://postgres:postgres@localhost:5432/venture_route_test`; `scripts/e2e_reset.py` before the Clerk suite.

**Contracts (`packages/contracts/src/marketplace.ts`)**: `RequestCreate`, `Request`, `RouteSnapshot`, `Eligibility`, `BidCreate`, `Bid`, `BookingProposal`, `BookingCreate`, `Booking`, `BookingHistoryEntry`, `Dashboard`, `DashboardCounts`, `RouteCounts`, `DecidedQueue`, plus `Ecosystem` in `src/ecosystem.ts`; parity test and `schema.json` regenerated by `scripts/export_schema.py`.

**Web (`apps/web`)**: routes `/requests`, `/dashboard`, `/bookings/new`, `/bookings/:bookingId`, `/partners`, `/privacy` next to the Sprint 003 screens; `MarketplaceApi` (`src/api/marketplace.ts`) with requests, bids, bookings, dashboard, decided and a typed `ApiForbiddenError` (403 reason) — Showcase calls join it; the routing store `hydrate` action for a stored brief and route; the "Sign in to publish" stash across sign-in; `test/fakeMarketplace.ts` and the injected `auth` / `marketplace` App props for RTL; the Clerk Playwright suite (`playwright.clerk.config.ts`, `e2e/clerk/*`, engine 8001, preview 4175) with `requests.spec.ts` as the round-trip model; the header and footer with `ScrollToHash`, the Ecosystem links and the Partners and Privacy pages (#79).

**CI (D-33 as amended by D-49)**: the web job runs the Clerk suite against a `postgres:18` service container when the two repository secrets exist and skips with a message otherwise; workflow edits push over SSH.

**Operator state**: the Clerk session-token `metadata` claim is in place; `VITE_CLERK_PUBLISHABLE_KEY` and `CLERK_SECRET_KEY` are repository secrets; the Neon `dev` branch is at migration `0002`; `.env` holds real Clerk and Neon values (never printed).

**Open questions**: Q-17 (Showcase Stitch export), Q-18 (demo Showcase content and consent), Q-19 (pitch deck PDF), Q-20 (`/showcase` public or signed-in; D-43 assumes public), Q-21 (founder confirmation before publishing), Q-22 (founder name on bids and bookings).

---

## Part A — Builder Showcase

Seams (fixed): HTTP `PUT /api/me/projects/{id}/showcase`, `POST /api/me/credentials`, `GET /api/showcase`, `GET /api/showcase/{id}`, `/api/admin/{confirm|reject}/showcase/{id}` against `TEST_DATABASE_URL` (fake JWT per role, no token for public routes); pure functions `validate_https_url`, `youtube_video_id` and the `render_program()` byte-equal invariant; rendered screens through RTL; Playwright no-key `showcase.spec.ts` with `page.route` stubs; Clerk `showcase.spec.ts` (builder → admin → signed-out visitor).

Skills: superpowers `brainstorming`, `writing-plans`, `subagent-driven-development`, `test-driven-development`, `verification-before-completion`; Matt Pocock `to-spec`, `to-tickets`, `implement`, `tdd`, `code-review`; `neon-postgres`, `secure-coding` (URL rules, embeds), `clerk-testing`, `vercel-react-best-practices`, `vercel-composition-patterns`, `web-design-guidelines` (final audit), `playwright-cli`.

### A0 — Operator, before P1
Merge the `#49` PR if it is still open. Answer Q-17, Q-18 and Q-20 in `planning/QUESTIONS.md` or accept their defaults (Builder composes from batch-4 requests-board, batch-3 candidate-profile and batch-2 landing; three fictional Demo data entries with `https://example.org/…` links; `/showcase` public).

### P1 — Orient
```text
You are the Builder for Venture Route, Sprint 005 Part A — Builder Showcase. Fetch origin. If PR "Sprint 004: Requests and interviews" (#78) is not merged into master, tell me and stop. Otherwise create and check out branch sprint/005a-showcase from origin/master (D-42), not sprint/005-demo-hardening.

Read, in order: AGENTS.md, CLAUDE.md, CONTEXT.md, planning/STATE.md, planning/DECISIONS.md (D-39 to D-49), planning/DOMAIN.md, planning/TIMELINE.md, planning/sprints/004-requests-interviews/review.md, then planning/sprints/005-demo-hardening/requirements.md §Part A, blueprint.md §Part A and acceptance.md §Part A, and this file's inheritance section.

Reply with: (1) the Part A goal in two lines, (2) the seams we will test at, taken from PROMPTS-005.md §Part A, (3) which of Q-17, Q-18, Q-20 are still open and the default you will follow for each, (4) every other question you cannot answer from the pack. Do not write code. Wait for me.
```

### S0 — Brainstorm the Showcase (superpowers `brainstorming`)
```text
Use the brainstorming skill. This is an architectural change (new migration, public endpoints, a new admin kind): say so. Ground everything in planning/sprints/005-demo-hardening/requirements.md §Part A, blueprint.md §Part A, acceptance.md §Part A, CONTEXT.md, docs/adr/ and D-42, D-43. D-43 is settled: do not reopen display-only, admin gating, public read or the URL rules. Write back your understanding first, separating what the pack says from what you assume. Ask me one question at a time, multiple choice, only where the pack is silent — expected: card layout and fields (Q-17 fallback exports), empty and pending states wording, filter chip set, sort order of the gallery (default: most recently confirmed first), whether seed licensable IP assets (asset-afya-triage, asset-shamba-records, asset-elimu-quiz; they are already on /partners) get read-only cards. Present the design in sections (data, API, visibility, web screens, admin, demo seed, tests) and stop for my approval after each. Do not write files; when I approve the last section, stop and wait for /to-spec.
```

### S1 — Publish the spec
```text
/to-spec

Synthesize the approved Showcase design plus planning/sprints/005-demo-hardening/requirements.md §Part A and blueprint.md §Part A into the spec template. Seams are the Part A seams in PROMPTS-005.md; confirm them with me before writing. User stories 1–6 from requirements.md §Part A; Implementation Decisions cite D-40, D-42, D-43 and reuse the Sprint 004 pieces named in the inheritance section (MarketplaceApi, admin kinds, the cast fixtures, the Clerk suite); Testing Decisions cite the seams and each acceptance.md §Part A line; Out of Scope copies requirements.md §Out of scope (Part A). Publish as one GitHub issue "Spec: Sprint 005a — Builder Showcase" labelled sprint:005 and ready-for-agent. Give me the issue number.
```

### P2 — Slice
```text
/to-tickets #<spec issue>

Sprint label: sprint:005. Follow blueprint.md §Part A Steps 1–15 in that order and keep its blocking brackets as native GitHub relationships; prefactors (links module, contracts, MarketplaceApi + routes + TopNav link) are their own tickets. Tracer-bullet slices, each demoable and one fresh context window. Every ticket quotes the acceptance.md §Part A line it proves. #49 is merged; its Should line becomes "reverse a showcase decision" inside the admin Showcase tab ticket. Show me the numbered breakdown with the wave each ticket falls in (blueprint §Wave plan) and wait for my approval before publishing.
```

### P3-fast — Build in waves (superpowers `subagent-driven-development`, D-41)
```text
You are the Sprint 005a controller on branch sprint/005a-showcase (never master). Use the superpowers subagent-driven-development skill for the ledger, task briefs, review packages and per-task review loop; Matt Pocock tdd and code-review inside implementers and reviewers; verification-before-completion before any claim. Read AGENTS.md, CONTEXT.md, D-39 to D-49, planning/sprints/005-demo-hardening/{requirements,blueprint,acceptance}.md §Part A and PROMPTS-005.md once.

Setup (20 min): fetch spec #<spec issue> and every open sprint:005 issue with gh; write .superpowers/sdd/sprint-005a-plan.md with one "### Task N — #<issue> <title>" per ticket in wave order (W0 migration, links · W1 contracts, projection guard, builder writes · W2 public read, admin kind, web prefactor · W3 gallery, detail + PitchVideo, profile, admin tab · W4 demo seed, Playwright), each with the issue body verbatim, Blocked by, the acceptance lines it quotes and its seam. Global constraints: showcase data never reaches the MeTTa space and render_program() stays byte-equal (D-43); visibility is one SQL predicate (showcased ∧ showcase_status confirmed ∧ project confirmed ∧ account confirmed); showcase edits reset to pending; admin showcase decisions never call reproject(); https-only links, no localhost/IP, ≤500 chars, YouTube-only pitch with a youtube-nocookie embed after click, rel="noopener noreferrer"; public routes need no token, builder writes are owner-only (404 otherwise), 401/403 never 500; every new column respects demo_data; Zod mirrors with the parity test; secrets from .env only; no planning files on this branch. Pre-flight conflict table for the shared files (models.py, schemas.py, repo.py, router.tsx, marketplace.ts, TopNav.tsx, AdminHome.tsx, test_schemas.py, schema-parity.test.ts): rule on each row in the ledger, and regenerate schema.json once per wave, not per ticket.

Per wave: one implementer subagent per ticket with isolation "worktree" on branch sprint/005a/t<issue> (engine, migration and security tickets at effort high; prefactor and pure-UI tickets on a lighter model). Each implementer uses its own test database (create venture_route_test<issue> on the compose db) so concurrent pytest sessions never drop each other's schema. Red test first at the ticket's seam, green, typecheck, one commit "<type>(<scope>): <what> (#<issue>)", report RED and GREEN output. Merge the wave's branches in ascending issue order, resolve conflicts (ledger ruling), run the full suite once (uv run pytest -q with TEST_DATABASE_URL, ruff, uv run mypy ., pnpm -r typecheck, pnpm -r lint, pnpm -r test with --no-file-parallelism for the web), dispatch the wave's task reviewers in parallel, fix rounds max 5. Close each issue with command, output and SHA. Time budget: W0 60 min, W1 90, W2 90, W3 120, W4 90; 50 percent over → ledger which ticket slips and apply the Part A scope floor in blueprint.md if G5a (Sun 27 Sep 20:00) is at risk.

Finish: final whole-branch review with spec #<spec issue> and acceptance.md §Part A as the Spec axis; one fix dispatch, one re-review. Stop only for the four superpowers stop conditions; pushes to the sprint branch are pre-approved.
```

### P4 — Review
```text
/code-review master

Spec sources: the spec issue, planning/sprints/005-demo-hardening/acceptance.md §Part A and the closed sprint:005 issues. Standards sources: AGENTS.md §Non-negotiables and §Conventions, CONTEXT.md, docs/adr/, D-43, D-49. Also run the secure-coding skill over the showcase endpoints, links.py and PitchVideo, and the web-design-guidelines skill over /showcase, /showcase/:projectId and the profile editor. Fix every hard violation and every Spec finding now, each in its own commit referencing the issue; list the judgement-call smells you chose not to fix and why.
```

### P5 — Evidence and PR
```text
Use the verification-before-completion skill. Run every command in planning/sprints/005-demo-hardening/acceptance.md §Part A from a clean checkout of this branch and paste the real output under each line, ✅ or ❌; the Clerk suite runs locally against the compose db with the engine on 8001 and the preview on 4175, never concurrently with another Clerk run. Update planning/STATE.md (Sprint 005a shipped, what did not, what Part B inherits). Push the branch and open one pull request titled "Sprint 005a: Showcase" against master whose body follows planning/AUTOMATION.md §Completion report (all eight items). Keep it a draft if any Must line is ❌. Give me the PR URL.
```

---

## Part B — Demo hardening

Seams: `docker compose up` end-to-end via Playwright `demo.spec.ts`; deployed `/health` and landing page via HTTP checks; every earlier acceptance suite on the freeze commit.

Skills: `gem-devops-guidelines`, `docker-build-strategies`, `docker-compose-patterns`, `use-railway` and `deploy-to-vercel` **for writing `docs/DEPLOY.md` only**, `neon-postgres` (production `main` branch, `demo` branch for resets), `playwright-cli` (recording), `secure-coding` (headers, CORS, secrets), `mattpocock-skills:wizard` (turn DEPLOY.md into the step-by-step wizard the Operator runs).

### P1b — Orient
```text
You are the Builder for Venture Route, Sprint 005 Part B — Demo hardening. Fetch origin. If PR "Sprint 005a: Showcase" is not merged and it is before Mon 28 Sep 08:00, tell me and stop; after that time proceed under the Part A scope floor in blueprint.md. Create sprint/005-demo-hardening from origin/master. Read AGENTS.md, CLAUDE.md, CONTEXT.md, planning/STATE.md, planning/DECISIONS.md (D-27, D-28, D-42 to D-49), planning/TIMELINE.md, then planning/sprints/005-demo-hardening/requirements.md §Part B, blueprint.md §Part B and acceptance.md §Part B. Reply with the goal in two lines, the Part B seams, whether the Hyperon init in Docker (c025647) is on master, the current reprojection timing on Neon dev (#51), and every open question (Q-08, Q-19). No code. Wait for me.
```

### P2b — Slice
```text
/to-tickets planning/sprints/005-demo-hardening/requirements.md

Part B only. Sprint label sprint:005. Order fixed by blueprint.md §Part B Steps 1–8: regressions (one ticket per failing acceptance line found by running every suite 000–005a on master) → compose web service + README launch → #51 (reuse the issue) → demo.spec.ts + docs/DEMO.md (five scenarios, change-a-constraint, the marketplace moment: builder profile → admin confirm → route includes the builder, the request → bid → booking round-trip, the Showcase) → recording → docs/PITCH.md + Slides deck → docs/DEPLOY.md wizard → docs/SUBMISSION.md + freeze. Quote the acceptance.md §Part B line in each. Show me the breakdown and wait.
```

### P3b — Implement
```text
/implement Work the frontier of GitHub issues labelled sprint:005 and ready-for-agent, in dependency order, one ticket at a time. For each ticket: assign it to me, use /tdd only at the seams in PROMPTS-005.md §Part B, red then green with the output pasted, typecheck after every green, one commit per ticket in Conventional Commits form ending with "(#<issue>)", then close the issue with the test command and output. Stop and ask me if a ticket needs a decision that is not in planning/DECISIONS.md or CONTEXT.md.

No new features. Never run railway or vercel yourself: docs/DEPLOY.md is a wizard for me — Railway steps for services/engine (Dockerfile, release command alembic upgrade head, health check /health, env vars from .env.example including the Clerk trio and ADMIN_EMAILS, public domain, CORS_ORIGINS with the Vercel URL), Vercel steps for apps/web with VITE_API_URL and VITE_CLERK_PUBLISHABLE_KEY, Neon production branch as DATABASE_URL with DATABASE_URL_DIRECT for the release command, the Clerk session-token claim and the production webhook with its signing secret; stop and wait while I run each command and paste the URL and /health output back, then record them in README. Record demo.spec.ts only after it passes twice against a clean docker compose up. Freeze: tag v0.1.0-demo on the merged commit and write the SHA into STATE.md with "frozen for demo".
```

### P4b — Review
The generic P4 with Spec sources acceptance.md §Part B and the closed sprint:005 Part B issues; also the `secure-coding` skill over the compose file, the Dockerfile and `docs/DEPLOY.md` (no secret ever written into a tracked file).

### P5b — Evidence and PR
```text
Use the verification-before-completion skill. Run every command in planning/sprints/005-demo-hardening/acceptance.md §Part B from a clean clone (cp .env.example .env, docker compose up) and paste the real output under each line, ✅ or ❌. Update planning/STATE.md ("frozen for demo", the tag and SHA, the deployed URLs). Push the branch and open one pull request titled "Sprint 005: Demo hardening" against master whose body follows planning/AUTOMATION.md §Completion report. Keep it a draft until my sign-off comment on docs/DEMO.md. Give me the PR URL.
```

### Architect review (paste into the Architect session, not the Builder)
```text
Builder Review for Sprint 005 <Part A | Part B>: read PR <url> on branch <sprint/005a-showcase | sprint/005-demo-hardening>, re-run every acceptance.md §<Part> command from the PR head, compare with the completion report, and give the verdict DONE / DONE_WITH_FOLLOW_UPS / NOT_DONE with the failing lines quoted. Write planning/sprints/005-demo-hardening/review-<a|b>.md in the style of planning/sprints/004-requests-interviews/review.md, then update STATE.md.
```

### Resume (if a session dies mid-part)
```text
Resume Sprint 005 <Part A | Part B> on branch <sprint/005a-showcase | sprint/005-demo-hardening>. Read AGENTS.md, CONTEXT.md, planning/STATE.md, planning/sprints/005-demo-hardening/acceptance.md §<Part>, PROMPTS-005.md, then `git log origin/master..HEAD --oneline` and the open sprint:005 issues. Tell me which tickets are closed, which is in progress and its last green test, and what is next. Then continue with P3-fast (Part A) or P3b (Part B).
```
