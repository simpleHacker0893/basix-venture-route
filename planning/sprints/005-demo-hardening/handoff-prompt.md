Operator: drive this sprint with the paste-ready prompts in `planning/PROMPTS.md` §Sprint 005 (Part A: C0 → P1 → S0 → S1 → P2 → P3-fast → P4 → P5; Part B: P1b → P2b → P3b → P4 → P5b). This file is the cloud-Routine variant of the same instructions.

You are the Builder for Venture Route, Sprint 005. Before any work, fetch origin and read `planning/STATE.md`.

**Part A (Showcase).** If PR "Sprint 004: Requests and interviews" is not merged, comment that Sprint 005 is waiting and stop. Otherwise create `sprint/005a-showcase` from `master`. Read the pack, then this folder's `requirements.md` §Part A, `blueprint.md` §Part A and `acceptance.md` §Part A, and D-39 to D-43. Brainstorm with the superpowers `brainstorming` skill, publish the spec with `/to-spec`, slice with `/to-tickets`, and build in waves with `subagent-driven-development` + `tdd` at the blueprint seams. Review with `/code-review master`, then open PR "Sprint 005a: Showcase".

**Part B (hardening).** After 005a merges (or at Mon 28 Sep 08:00 under the Part A scope floor), create `sprint/005-demo-hardening` from `master`. No new features. Load `gem-devops-guidelines`, `docker-compose-patterns` and `docker-build-strategies`. Fix regressions first, then the launch command, `DEMO.md`, recording, `PITCH.md`, the `DEPLOY.md` wizard and `SUBMISSION.md`. Never run `railway` or `vercel`. Open PR "Sprint 005: Demo hardening"; the Operator's sign-off comment on the demo script is part of acceptance.
