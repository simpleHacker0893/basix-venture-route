# STATE — rolling snapshot (edit in place, never append a log)

**Updated:** 2026-09-24 (afternoon, Architect) · **Demo:** Thu 2026-10-01 · **Current sprint:** 004 — Requests and interviews **merged** (PR #78 → `master` at `a091af7`, Operator instruction of 2026-09-24 recorded in `planning/sprints/004-requests-interviews/review.md`; the CI Clerk step ran with the repository secrets, see the review for its state). Sprint 003's #47 lines are proven by the same Clerk suite. **Next:** Sprint 005 split by D-42 into 005a Showcase (Fri 25 → Sun 27 Sep) and Part B demo hardening (Mon 28 → Wed 30 Sep, freeze 22:00); prompts in `planning/sprints/005-demo-hardening/PROMPTS-005.md`.

## Where we are
- Sprints 000–002 are on `master`: the FastAPI engine with `hyperon==0.2.10` in-process (seven named rules over 181 seed atoms), the routing core (`POST /api/route`, `GET /api/scenarios`, `POST /api/conversation`, LLM adapters with the null fallback), `packages/contracts` (Zod 4 with the Pydantic parity test) and the founder UI (`/`, `/route`, `/handoff`, Why this route? drawer, PWA, offline mode) replicating the Stitch exports (D-36).
- Sprints 003 and 004 are on `master` too: the marketplace (Clerk roles, Neon Postgres store, profiles, credentials, projects, admin confirmation projected into the graph) and requests, bids, interview bookings and the founder dashboard. Q-15 and Q-16 closed by D-39 and D-40.
- CI (D-33): the `engine` job (uv sync, pytest against a `postgres:18` service container with `TEST_DATABASE_URL`, `alembic upgrade head`, ruff, `mypy .`, both `--check` scripts) and the `web` job (build, typecheck, lint, Vitest, the engine on `LLM_PROVIDER=null`, the no-key Playwright suite against `vite preview`, HTML report as an artifact). Every push of the sprint branch has run green; the run for the PR head is linked in the PR #48 report.
- Store: Neon project `venture_route` (D-17, PostgreSQL 18 per D-37) with `production`, `dev` and `test` branches. `.env` now carries the real `DATABASE_URL` (dev, pooled), `DATABASE_URL_DIRECT` (dev, direct, for Alembic) and `TEST_DATABASE_URL` (test branch) in the asyncpg `?ssl=require` form, written from the Neon CLI on 2026-09-23 without printing them; the `dev` branch is migrated to `0001`. The compose `db` (postgres:18) runs locally on 5432 with `venture_route` (migrated) and `venture_route_test`; Docker Desktop must be running for it.
- Clerk (D-03): `VITE_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`, `CLERK_JWKS_URL` and `ADMIN_EMAILS` are real in `.env` (dev instance; hostnames stay out of tracked files, AGENTS.md rule 8). `CLERK_WEBHOOK_SIGNING_SECRET` is still the placeholder: the Clerk Playwright suite signs its own events with a per-run secret, so the placeholder only matters for a real Dashboard webhook.
- Windows notes: pushes that touch `.github/workflows` go over SSH (the `gh` token lacks the `workflow` scope); `pytest.exe` stubs outside the repo are blocked by Application Control (use `python -m pytest` / `uv run pytest`); clean checkouts and worktrees must live at a short path (`C:\w\…`); the web Vitest suite is run serially (`--no-file-parallelism`) when other suites share the machine, otherwise it times out.
- Prompt 0, D-17 (Neon), D-25 (React 19), D-26 (keys via `.env`), D-27 (Operator runs Railway/Vercel), D-28 (demo assets), D-36 (Stitch exports win), D-37 (postgres:18), D-38 (Chloe voice, post-demo) stand. Docker is verified on GitHub Codespaces and locally. Knowledge graph: `graphify-out/` committed in `f04ede7`.

## Sprint 004 — merged (PR #78, `a091af7`; review in `planning/sprints/004-requests-interviews/review.md`)
- **Shipped** (tickets #53–#77 closed with RED/GREEN evidence): migration `0002` (`requests`, `bids`, `bookings`, `demo_data` CHECKs); wire schemas and Zod mirrors with parity; `RouteService.eligibility` from `eligible_builders` + `gaps` with the two-tier reason (D-45); slot rules and the Africa/Nairobi presenter (D-46); the founder-owned booking machine as pure `start`/`check`/`transition` (D-44) with JSONB history written in the transition's transaction (D-48); requests API with the route snapshot (D-47), eligibility endpoint, close → 409 on bids; bids behind the 403 gate; bookings accept / counter / confirm; `GET /api/me/dashboard` with SQL counts and an injectable clock; web `/requests`, `/dashboard`, `/bookings/new`, `/bookings/:id`, "Publish as request", the sign-in stash; the Clerk Playwright round-trip (`requests.spec.ts`, 14/14 locally) and the CI Clerk step (D-49).
- **Review fixes on the branch before the merge**: master merge (`19cc454`, keeping #80's `/partners` and `/privacy` over the branch's duplicates), the dashboard clock (`6c41282`), a founder counter opening a new round and 409 before 422 on a confirmed booking (`0db1dd7`), README routes (`68656c3`). Judgement calls left for 005a are listed in the review.
- **Follow-ups**: #82 (Must 1 browser proof with the Health brief), #49 (Reverse from `/admin`, PR #83), #51 (reprojection latency on Neon, Part B), Q-21 and Q-22 for the Operator. The CI Clerk suite is flaky on GitHub runners (sign-in steps time out at 20 s on some runs); D-49 keeps it in the job, the review records the runs.
- **Also on master**: #80 (header section links, Ecosystem and Privacy pages), #81 (D-39 to D-43, Sprint 005 plan). Not on master: `c025647` on `antonypeter` (Hyperon init in Docker) — Part B checks it.

## Sprint 003 — merged (PR #48 at `5ff6a31`, Operator override; #47 proven later by the Clerk suite)
- Store, Clerk auth and webhook, marketplace API (`/api/me/*`, `/api/builders/{id}`, `/api/admin/*`), the projection (`reproject()` after every decision, `projected_rows` on `/health`), the web sign-in frame, role select, `RequireRole`, `/profile`, `/builders/:id` and `/admin`; details in the PR #48 report and `docs/API.md`. The review fixes of 2026-09-23 (slug vocabulary, engine lock, JWKS throttle, webhook and role-write ordering) are on master. Judgement calls kept: wire shape `{id, kind, status, projectedRows}`, availability `start == end` allowed, `projection.ts` mirrors `projection.py` for the admin preview, 8-character `cred-`/`proj-` atom ids, marketplace tests skip without `TEST_DATABASE_URL`.

## Blockers
- Q-17 (Showcase Stitch export), Q-18 (demo showcase content and consent), Q-20 (public Showcase) before 005a S0; Q-19 (pitch PDF) and Q-08 (team names) before Part B; Q-21 and Q-22 have shipped defaults.
- `CLERK_WEBHOOK_SIGNING_SECRET` stays the placeholder until the production webhook exists (Part B `docs/DEPLOY.md`).
- The Neon `dev` branch must be migrated to `0002` before any run against it (`ALEMBIC_DATABASE_URL=<direct url> uv run alembic upgrade head`); the compose `db` is at `0002`.

## What Sprint 005 inherits
- **Store**: Alembic `0001_marketplace` and `0002` (`requests` with the route snapshot, `bids` UNIQUE per builder and request, `bookings` with JSONB history); `demo_data` default true + CHECK everywhere; `0003_showcase` is the next revision. Alembic resolves `ALEMBIC_DATABASE_URL`, then `DATABASE_URL_DIRECT`, then `DATABASE_URL`; CI upgrades the `postgres:18` service container; pytest drops and upgrades `TEST_DATABASE_URL` once per session and rolls every test back. Concurrent pytest sessions need their own database (`venture_route_test<issue>` on the compose `db`).
- **Endpoints**: everything in `docs/API.md` §index: routing and `GET /api/ecosystem` (public), `/api/me/*`, `/api/builders/{id}`, `/api/admin/pending|decided|confirm|reject` (kinds `account | credential | project`; 005a adds `showcase`), `/api/requests*`, `/api/me/bids`, `/api/bookings*`, `/api/me/dashboard`, `/api/webhooks/clerk`, `/health.projected_rows`.
- **Engine seams**: `RouteService.eligibility(brief, builder_id)` (D-45); the booking machine `app/marketplace/booking.py` and `slots.py` (D-44, D-46); `reproject()` after any commit that changes confirmed rows, whole under the engine lock (D-15) — Showcase decisions never call it (D-43); `create_app(clock=…)` for time-dependent handlers; the cast fixtures (`founder`, `admin`, `confirmed_builder`, `unconfirmed_builder`, `rejected_builder`, `TEST_NOW`) in `tests/conftest.py`; `scripts/e2e_reset.py` before a Clerk suite run.
- **Web**: `MarketplaceApi` (`apps/web/src/api/marketplace.ts`, typed 404 and 403 errors) and `fakeMarketplace`; the injected `auth` / `marketplace` App props; `RequireRole` and `ROLE_HOME`; the routing store `hydrate` action; `ScrollToHash`, the Ecosystem footer links, `/partners` and `/privacy`; `StatusPill`, `DemoDataPill`, `EvidenceBadge`; the Clerk harness (`playwright.clerk.config.ts`, engine 8001, preview 4175, `e2e/clerk/*`) with `requests.spec.ts` as the round-trip model; Stitch: batch-4 exports converted, no Showcase export (Q-17).
- **CI (D-33, D-49)**: engine job with the Postgres service; web job with the no-key suite always and the Clerk suite when the two repository secrets exist (present since 2026-09-24).
- **Operator state**: Clerk session-token `metadata` claim set; `VITE_CLERK_PUBLISHABLE_KEY` and `CLERK_SECRET_KEY` are repository secrets; `.env` holds real Clerk and Neon values (never printed); Docker Desktop with the compose `db` (`venture_route` at `0002`, `venture_route_test`, `venture_route_test49`).

## Next
1. Merge the #49 PR (Reverse from `/admin`), then start Sprint 005a from `master` with `planning/sprints/005-demo-hardening/PROMPTS-005.md` §Part A (A0 → P1 → S0 → S1 → P2 → P3-fast → P4 → P5); gate G5a Sun 27 Sep 20:00.
2. Sprint 005 Part B from Mon 28 Sep 08:00: P1b → P2b → P3b → P4b → P5b; #51 measured on Neon `dev`; freeze Wed 30 Sep 22:00, tag `v0.1.0-demo`.
3. #82 (Health-brief browser proof) rides with the 005a Clerk spec; #19 stays optional.
4. Post-demo: Sprint 006 — Chloe voice intake (D-38).

## Scope floor (must be on screen on 1 Oct)
Chat/form intake · brief review chips · route result with gaps first · Why this route? drawer · five demo scenarios · Demo data labels · one launch command.
