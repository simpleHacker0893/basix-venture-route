/** Placeholder builder home (Sprint 003 #44); ticket #45 replaces it with the profile editor. */
export function BuilderHome() {
  return (
    <div className="mx-auto w-full max-w-[1200px] px-6 py-12">
      <h1 className="font-display text-3xl font-semibold text-ink">Your profile</h1>
      <p className="mt-3 max-w-xl text-sm leading-relaxed text-ink-muted">
        Your builder profile, credentials and projects arrive with the next ticket.
      </p>
    </div>
  );
}
