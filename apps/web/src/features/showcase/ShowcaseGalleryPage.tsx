/**
 * /showcase (spec #86 stories 30-38): a public gallery of admin-approved builder projects, no
 * sign-in needed. Placeholder for #104, which replaces this body with the card grid, filters and
 * paging wired to `MarketplaceApi.showcase.list`.
 */
export function ShowcaseGalleryPage() {
  return (
    <div className="mx-auto w-full max-w-[1200px] px-6 py-16">
      <h1 className="font-display text-4xl leading-tight text-ink">Showcase</h1>
    </div>
  );
}
